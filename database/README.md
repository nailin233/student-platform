# Day 2 数据库

启动 Docker Desktop 后，在项目根目录运行 `docker compose up -d mysql`。MySQL 会自动执行 `database/schema.sql`。
连接信息：主机 `127.0.0.1`，端口 `3306`，数据库 `student_platform`，用户 `root`，密码 `root`。

Day 2 只完成建表与演示数据；业务 CRUD 将在 Day 3 开始。
