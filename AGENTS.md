# 学员管理平台开发约束

## 使用规则

每次开始修改代码前，必须先阅读本文件，并根据当前阶段检查任务是否符合范围。
如果用户要求的内容超出当前阶段、可能破坏已完成流程、涉及不可逆数据操作或会明显扩大 Demo 范围，必须先提醒用户，说明影响，再等待明确指示；不要一股脑执行。

每完成一个模块，必须说明：完成了什么、如何运行、如何测试、当前风险和下一步。
代码应保持简单、稳定、可演示，优先主流程，避免过度设计。

## 项目目标

项目名称：培训机构学员管理平台 Demo V1.0。
目标：14 天完成可以向客户展示的 MVP，不是正式商业版本。

用户端：微信小程序报名、查看课程和老师、填写报名资料。
管理端：学员、课程、老师、缴费信息管理，搜索筛选，Excel 导出。

核心闭环：

管理员维护课程和老师 → 学员查看课程 → 学员提交报名 → 后台出现学员记录 → 管理员查询、修改、导出。

## 技术栈

- 前端：Vue 3、Element Plus、Vite
- 后端：FastAPI
- 数据库：MySQL 8.4
- 数据访问：SQLAlchemy、PyMySQL
- 部署：Docker Compose
- 开发环境：Windows；目标部署环境：Linux

## PRD 核心字段

学员必须支持：

- 姓名
- 性别
- 年龄
- 联系电话
- 所选课程
- 任课老师
- 上课时间

扩展字段：

- 紧急联系人
- 紧急联系电话
- 缴费时间
- 缴费金额
- 学制
- 备注

管理能力：新增、修改、删除、查询、分类查看、学员详情、学员 Excel 导出、缴费信息导出。

课程示例：书法班、摄影班、声乐班。课程需要支持新增和维护。
老师需要支持新增和维护，并能与课程/学员关联。

## 14 天阶段边界

### Day 1
项目初始化、环境检查、目录结构、前后端启动、健康检查。

### Day 2
MySQL 数据库设计、建表、演示数据、数据库连接验证。

### Day 3-5
学员管理：CRUD、搜索、分页、详情、完整 PRD 字段。

### Day 6
课程管理、老师管理、课程与老师关联。

### Day 7
学员详情页面和管理员工作台、服务状态统计。

### Day 8
Excel 导出：学员信息和缴费信息。

### Day 9-10
微信小程序 Demo 报名流程。可以先使用模拟数据或开发者工具，不把正式上线、HTTPS、备案、真实支付混入 Demo。

### Day 11
UI 优化：简单、大字体、清晰操作，适合中老年用户。

### Day 12
Docker 完整部署和 Linux 部署说明。

### Day 13
测试、主流程回归、错误修复。

### Day 14
客户演示数据、演示流程、README 和交付检查。

## 当前状态

已完成或基本完成：

- 项目初始化和前后端启动
- MySQL Docker 容器和数据库表
- 学员 API 和完整管理页面（含完整 PRD 字段）
- 课程、老师管理（含新增、编辑、删除）
- 管理员工作台统计
- Excel 导出：学员信息（15 列）、缴费信息（6 列）
- 微信小程序：课程 / 报名 / 老师 / 我的 四个 Tab
- Docker 部署（MySQL + 后端容器）
- 接口层回归脚本（63 项断言，全部通过）
- 演示数据种子脚本
- 客户演示流程文档、部署文档

当前阶段：**Day 14 交付收尾 + 云服务器部署上线**。功能层面主闭环已完整，
正在做上线部署验证（目标服务器 49.233.204.148，2核2G + 宝塔）。

当前优先事项：

1. 按 docs/DEPLOY-STEPS-49.233.204.148.md 完成服务器上线。
2. 交付前按 docs/DEMO-SCRIPT.md 走一遍完整演示流程。
3. 保持演示数据基线稳定（学员 6 / 课程 3 / 老师 3 / 待收 ¥2120）。
4. 如进入正式使用，按 docs/DEPLOYMENT.md「七、安全加固」逐项处理。

## 部署前必查（已经踩过的坑）

这两个问题都只在**全新环境**才暴露，本机有历史残留所以看不出来。
每次准备部署到新机器前，先确认：

**坑 1 · package.json 缺依赖声明（已修复）**

`frontend/package.json` 曾经漏了 `naive-ui`，但 `src/App.vue` 一直在 import 它。
本机因为 node_modules 有历史残留能跑，**全新环境 npm install 不会装 naive-ui，
vite build 直接失败**。已补上声明并重装生成 package-lock.json。

验证方法（新增依赖后必须做）：

```bash
cd frontend && rm -rf node_modules && npm install && npm run build
# 必须能产出 dist/index.html，否则就是有依赖没声明
```

**坑 2 · schema.sql 中文双重编码（已修复）**

见 docs/DEPLOY-BAOTA.md 坑 3。`schema.sql` 开头必须有 `SET NAMES utf8mb4;`。

## 当前阶段不做

除非用户明确确认，不要现在执行：

- 正式微信支付或真实收款
- 正式用户登录、复杂 RBAC 权限中心
- 微服务、Redis、消息队列、Kubernetes
- 生产级高可用、监控平台、复杂审计系统
- 大规模重构目录
- 删除数据库、删除 Docker 数据卷、清空演示数据
- 同时开发多个未完成模块
- 为了追求完整而加入 PRD 未要求的功能

## 修改前检查清单

1. 阅读本文件。
2. 判断用户要求属于哪一天、哪个模块。
3. 检查是否会影响已完成主流程。
4. 先备份或创建 Git 分支，尤其是重写核心文件前。
5. 小步修改并构建/运行验证，不要一次性堆叠未经验证的修改。
6. 遇到编码、数据库、前端状态或接口错误，先定位根因再修复。
7. 完成后报告实际结果；没有验证成功时，不得声称已完成。

## 代码和数据原则

- API 输入必须校验，数据库写入使用参数绑定。
- 可选字段应提供明确默认值，避免 SQL 参数缺失。
- 课程、老师使用 ID 关联，不在学员表中重复维护名称。
- 演示数据可以修改，但修改前应说明范围；删除数据必须谨慎。
- 中文文件统一使用 UTF-8。
- 优先维护可读性，避免压缩成难以维护的一行代码。

## 开发终端约定

- 项目代码修改、前端构建、后端运行和 Git 操作优先使用 WSL Ubuntu 的 Bash，不使用 PowerShell 修改代码。
- Windows 项目路径 J:\student-platform-demo 在 WSL 中对应 /mnt/j/student-platform-demo。
- WSL 后端使用独立虚拟环境 ackend/.venv-linux，不要复用 Windows 的 ackend/.venv。
- WSL 中启动后端：source backend/.venv-linux/bin/activate && python -m uvicorn main:app --reload --port 8000。
- WSL 中构建前端：cd frontend && npm run build。
- 使用 WSL 写入中文文件时统一采用 UTF-8，并在构建后验证。
